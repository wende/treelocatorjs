import * as React from "react";

function Button(props) {
  return <button className="submit-button">{props.label}</button>;
}

let make = Button;

export { make };

//# sourceMappingURL=Button.res.js.map
